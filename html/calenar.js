if (choice === "January" || choice === "March" || choice === "May" || choice === "July" || choice === august || choice === "October" || choice === "December")

{ let days = 31; } else if (choice === "April" || choice === "June" || choice === "September" || choice === "November") 
{ days = 30; } 

else  (choice === "February") { days = 28; }


switch (choice) {
	case "white":
		update(white, black);
		break;
	case "black:
		update(black, white);
		break;
	case "purple"
		update(purple, white);
		break;
	case "yellow"
		update(yellow, black);
		break;
	case "psychedelic"
		update(lime, purple);
		break;
}